spring:
  profiles:
    active: test
  application:
    name: devops-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:192.168.2.158}:8848
ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000
