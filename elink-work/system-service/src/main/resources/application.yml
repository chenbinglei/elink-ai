spring:
  profiles:
    active: test
  application:
    name: system-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:192.168.2.158}:8848
ribbon:
  ReadTimeout: 30000
  ConnectTimeout: 30000
