spring:
  profiles:
    active: test
  application:
    name: scrontab-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:192.168.2.158}:8848
ribbon:
  ReadTimeout: 40000
  ConnectTimeout: 40000
